// la extension del archivo mjs es para que se use solamente ecmascriptmodules

 export const suma = (n1, n2) => {
    return n1 + n2;
  };
  
  export const resta = (n1, n2) => {
    return n1 - n2;
  };
  
 export const multiplicacion = (n1, n2) => {
    return n1 * n2;}

   // exports {suma, resta, multiplicacion} esta es la otra forma
   //export default suma otra forma